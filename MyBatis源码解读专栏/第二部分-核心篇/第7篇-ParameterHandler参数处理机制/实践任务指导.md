# 第7篇：ParameterHandler参数处理机制 - 实践任务指导

## 📋 实践任务概览

本文档提供详细的实践任务指导，帮助你通过动手实践深入理解ParameterHandler的工作机制。

---

## 🎯 任务清单

### 必做任务
- [x] 任务1：环境搭建与基础测试
- [x] 任务2：参数类型处理实践
- [x] 任务3：自定义ParameterHandler开发
- [x] 任务4：参数处理流程调试

### 选做任务
- [ ] 任务5：参数加密处理器实现
- [ ] 任务6：参数验证机制开发
- [ ] 任务7：性能优化实践

---

## 任务1：环境搭建与基础测试

### 1.1 任务目标

- 搭建MyBatis测试环境
- 验证ParameterHandler基本功能
- 理解参数设置流程

### 1.2 环境准备

#### 1.2.1 创建数据库表

```sql
-- 创建测试数据库
CREATE DATABASE IF NOT EXISTS mybatis_test DEFAULT CHARACTER SET utf8mb4;

USE mybatis_test;

-- 创建用户表
CREATE TABLE t_user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100),
    age INT,
    status INT DEFAULT 1,
    password VARCHAR(255),
    phone VARCHAR(20),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 插入测试数据
INSERT INTO t_user (name, email, age, status, password, phone) VALUES
('张三', 'zhangsan@example.com', 25, 1, 'password123', '13800138000'),
('李四', 'lisi@example.com', 30, 1, 'password456', '13900139000'),
('王五', 'wangwu@example.com', 28, 0, 'password789', '13700137000');
```

#### 1.2.2 配置mybatis-config.xml

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <settings>
        <!-- 开启驼峰命名转换 -->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
        <!-- 打印SQL日志 -->
        <setting name="logImpl" value="STDOUT_LOGGING"/>
    </settings>
    
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/mybatis_test?useUnicode=true&amp;characterEncoding=utf8&amp;serverTimezone=Asia/Shanghai"/>
                <property name="username" value="root"/>
                <property name="password" value="your_password"/>
            </dataSource>
        </environment>
    </environments>
    
    <mappers>
        <mapper resource="mapper/UserMapper.xml"/>
    </mappers>
</configuration>
```

#### 1.2.3 创建实体类

```java
package com.example.entity;

/**
 * 用户实体类
 */
public class User {
    private Long id;
    private String name;
    private String email;
    private Integer age;
    private Integer status;
    private String password;
    private String phone;
    
    // 构造方法
    public User() {}
    
    public User(String name, String email, Integer age) {
        this.name = name;
        this.email = email;
        this.age = age;
    }
    
    // Getters and Setters
    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }
    
    public String getName() { return name; }
    public void setName(String name) { this.name = name; }
    
    public String getEmail() { return email; }
    public void setEmail(String email) { this.email = email; }
    
    public Integer getAge() { return age; }
    public void setAge(Integer age) { this.age = age; }
    
    public Integer getStatus() { return status; }
    public void setStatus(Integer status) { this.status = status; }
    
    public String getPassword() { return password; }
    public void setPassword(String password) { this.password = password; }
    
    public String getPhone() { return phone; }
    public void setPhone(String phone) { this.phone = phone; }
    
    @Override
    public String toString() {
        return "User{id=" + id + ", name='" + name + "', email='" + email + 
               "', age=" + age + ", status=" + status + "}";
    }
}
```

#### 1.2.4 创建Mapper接口

```java
package com.example.mapper;

import com.example.entity.User;
import org.apache.ibatis.annotations.Param;
import java.util.List;
import java.util.Map;

/**
 * 用户Mapper接口
 */
public interface UserMapper {
    
    /**
     * 根据ID查询用户（单参数）
     */
    User findById(Long id);
    
    /**
     * 根据姓名和状态查询（多参数）
     */
    List<User> findByNameAndStatus(@Param("name") String name, @Param("status") Integer status);
    
    /**
     * 根据用户对象查询（对象参数）
     */
    List<User> findByUser(User user);
    
    /**
     * 根据Map参数查询
     */
    List<User> findByMap(Map<String, Object> params);
    
    /**
     * 插入用户
     */
    int insert(User user);
    
    /**
     * 批量插入（集合参数）
     */
    int batchInsert(@Param("users") List<User> users);
}
```

#### 1.2.5 创建Mapper XML

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">
    
    <resultMap id="userMap" type="com.example.entity.User">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="age" column="age"/>
        <result property="status" column="status"/>
        <result property="password" column="password"/>
        <result property="phone" column="phone"/>
    </resultMap>
    
    <!-- 单参数查询 -->
    <select id="findById" resultMap="userMap">
        SELECT * FROM t_user WHERE id = #{id}
    </select>
    
    <!-- 多参数查询 -->
    <select id="findByNameAndStatus" resultMap="userMap">
        SELECT * FROM t_user 
        WHERE name = #{name} AND status = #{status}
    </select>
    
    <!-- 对象参数查询 -->
    <select id="findByUser" resultMap="userMap">
        SELECT * FROM t_user
        <where>
            <if test="name != null">AND name = #{name}</if>
            <if test="status != null">AND status = #{status}</if>
            <if test="age != null">AND age = #{age}</if>
        </where>
    </select>
    
    <!-- Map参数查询 -->
    <select id="findByMap" resultMap="userMap">
        SELECT * FROM t_user
        <where>
            <if test="name != null">AND name = #{name}</if>
            <if test="minAge != null">AND age >= #{minAge}</if>
            <if test="maxAge != null">AND age &lt;= #{maxAge}</if>
        </where>
    </select>
    
    <!-- 插入 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user (name, email, age, status, password, phone)
        VALUES (#{name}, #{email}, #{age}, #{status}, #{password}, #{phone})
    </insert>
    
    <!-- 批量插入 -->
    <insert id="batchInsert">
        INSERT INTO t_user (name, email, age, status)
        VALUES
        <foreach collection="users" item="user" separator=",">
            (#{user.name}, #{user.email}, #{user.age}, 1)
        </foreach>
    </insert>
</mapper>
```

### 1.3 测试代码

```java
package com.example.test;

import com.example.entity.User;
import com.example.mapper.UserMapper;
import org.apache.ibatis.io.Resources;
import org.apache.ibatis.session.SqlSession;
import org.apache.ibatis.session.SqlSessionFactory;
import org.apache.ibatis.session.SqlSessionFactoryBuilder;

import java.io.InputStream;
import java.util.*;

/**
 * ParameterHandler基础功能测试
 */
public class ParameterHandlerBasicTest {
    
    public static void main(String[] args) throws Exception {
        // 1. 加载配置文件
        String resource = "mybatis-config.xml";
        InputStream inputStream = Resources.getResourceAsStream(resource);
        SqlSessionFactory factory = new SqlSessionFactoryBuilder().build(inputStream);
        
        try (SqlSession session = factory.openSession()) {
            UserMapper mapper = session.getMapper(UserMapper.class);
            
            System.out.println("=== ParameterHandler基础功能测试 ===\n");
            
            // 测试1：单参数
            testSingleParameter(mapper);
            
            // 测试2：多参数
            testMultipleParameters(mapper);
            
            // 测试3：对象参数
            testObjectParameter(mapper);
            
            // 测试4：Map参数
            testMapParameter(mapper);
            
            // 测试5：集合参数
            testCollectionParameter(mapper);
            
            session.commit();
            System.out.println("\n=== 所有测试完成 ===");
        }
    }
    
    private static void testSingleParameter(UserMapper mapper) {
        System.out.println("--- 测试1：单参数 ---");
        User user = mapper.findById(1L);
        System.out.println("查询结果: " + user);
        System.out.println("参数处理：Long类型直接使用\n");
    }
    
    private static void testMultipleParameters(UserMapper mapper) {
        System.out.println("--- 测试2：多参数(@Param) ---");
        List<User> users = mapper.findByNameAndStatus("张三", 1);
        System.out.println("查询结果: " + users.size() + " 条");
        System.out.println("参数处理：包装为ParamMap{name=张三, status=1}\n");
    }
    
    private static void testObjectParameter(UserMapper mapper) {
        System.out.println("--- 测试3：对象参数 ---");
        User queryUser = new User();
        queryUser.setName("李四");
        queryUser.setStatus(1);
        
        List<User> users = mapper.findByUser(queryUser);
        System.out.println("查询结果: " + users.size() + " 条");
        System.out.println("参数处理：通过MetaObject反射获取属性值\n");
    }
    
    private static void testMapParameter(UserMapper mapper) {
        System.out.println("--- 测试4：Map参数 ---");
        Map<String, Object> params = new HashMap<>();
        params.put("name", "王五");
        params.put("minAge", 20);
        params.put("maxAge", 30);
        
        List<User> users = mapper.findByMap(params);
        System.out.println("查询结果: " + users.size() + " 条");
        System.out.println("参数处理：从Map中获取键值对\n");
    }
    
    private static void testCollectionParameter(UserMapper mapper) {
        System.out.println("--- 测试5：集合参数(foreach) ---");
        List<User> users = new ArrayList<>();
        for (int i = 0; i < 5; i++) {
            User user = new User("批量用户" + i, "batch" + i + "@test.com", 20 + i);
            users.add(user);
        }
        
        int rows = mapper.batchInsert(users);
        System.out.println("插入结果: " + rows + " 条");
        System.out.println("参数处理：foreach生成额外参数__frch_user_X\n");
    }
}
```

### 1.4 预期输出

```
=== ParameterHandler基础功能测试 ===

--- 测试1：单参数 ---
查询结果: User{id=1, name='张三', email='zhangsan@example.com', age=25, status=1}
参数处理：Long类型直接使用

--- 测试2：多参数(@Param) ---
查询结果: 1 条
参数处理：包装为ParamMap{name=张三, status=1}

--- 测试3：对象参数 ---
查询结果: 1 条
参数处理：通过MetaObject反射获取属性值

--- 测试4：Map参数 ---
查询结果: 1 条
参数处理：从Map中获取键值对

--- 测试5：集合参数(foreach) ---
插入结果: 5 条
参数处理：foreach生成额外参数__frch_user_X

=== 所有测试完成 ===
```

---

## 任务2：参数处理流程调试

### 2.1 任务目标

- 理解DefaultParameterHandler.setParameters()执行流程
- 观察参数值获取的优先级策略
- 分析TypeHandler的类型转换过程

### 2.2 调试步骤

#### 2.2.1 设置断点

在以下位置设置断点：

```java
// 1. DefaultParameterHandler.setParameters()
org.apache.ibatis.scripting.defaults.DefaultParameterHandler#setParameters

// 2. 参数值获取
// 观察if-else优先级判断

// 3. TypeHandler.setParameter()
org.apache.ibatis.type.BaseTypeHandler#setParameter
```

#### 2.2.2 调试代码

```java
/**
 * 参数处理流程调试
 */
public class ParameterHandlerDebugTest {
    
    public static void main(String[] args) throws Exception {
        SqlSessionFactory factory = buildFactory();
        
        try (SqlSession session = factory.openSession()) {
            UserMapper mapper = session.getMapper(UserMapper.class);
            
            // 在此处设置断点，逐步跟踪
            User user = new User("调试用户", "debug@test.com", 30);
            user.setPassword("test123");
            user.setPhone("13800000000");
            
            // 执行插入，进入setParameters方法
            mapper.insert(user);
            
            session.commit();
        }
    }
}
```

#### 2.2.3 观察要点

**步骤1：观察ParameterMapping列表**
```java
List<ParameterMapping> parameterMappings = boundSql.getParameterMappings();
// 查看有哪些参数需要设置：name, email, age, status, password, phone
```

**步骤2：观察参数值获取优先级**
```java
// 对于每个参数，观察走哪个分支：
if (boundSql.hasAdditionalParameter(propertyName)) {
    // 额外参数（foreach生成）
} else if (parameterObject == null) {
    // 空参数
} else if (typeHandlerRegistry.hasTypeHandler(parameterObject.getClass())) {
    // 基本类型（单参数场景）
} else {
    // 复杂对象（使用MetaObject反射）
    value = metaObject.getValue(propertyName);
}
```

**步骤3：观察TypeHandler类型转换**
```java
// 查看不同类型参数使用的TypeHandler
TypeHandler typeHandler = parameterMapping.getTypeHandler();
// String -> StringTypeHandler
// Integer -> IntegerTypeHandler
// Long -> LongTypeHandler
```

**步骤4：观察PreparedStatement参数设置**
```java
// 最终调用
typeHandler.setParameter(ps, i + 1, value, jdbcType);
// 内部执行
ps.setString(1, "调试用户");
ps.setString(2, "debug@test.com");
ps.setInt(3, 30);
// ...
```

### 2.3 调试任务

完成以下调试任务并记录观察结果：

1. **任务2.1**：跟踪单参数查询的参数处理流程
2. **任务2.2**：跟踪对象参数的MetaObject反射过程
3. **任务2.3**：跟踪foreach集合参数的额外参数生成
4. **任务2.4**：观察不同类型参数的TypeHandler选择

---

## 任务3：自定义ParameterHandler开发

### 3.1 任务目标

开发一个参数加密处理器，自动对敏感字段进行加密。

### 3.2 实现步骤

参考"代码示例集合.md"中的EncryptionParameterHandler实现。

### 3.3 测试验证

```java
/**
 * 自定义ParameterHandler测试
 */
public class CustomParameterHandlerTest {
    
    public static void main(String[] args) throws Exception {
        // 配置自定义LanguageDriver
        Configuration configuration = new Configuration();
        configuration.setDefaultScriptingLanguage(EncryptionLanguageDriver.class);
        
        // ... 其他配置
        
        try (SqlSession session = factory.openSession()) {
            UserMapper mapper = session.getMapper(UserMapper.class);
            
            User user = new User("测试用户", "test@example.com", 25);
            user.setPassword("plainPassword");  // 明文密码
            user.setPhone("13800138000");       // 明文手机号
            
            mapper.insert(user);
            
            // 验证数据库中的数据已加密
            User savedUser = mapper.findById(user.getId());
            System.out.println("数据库中的密码（已加密）: " + savedUser.getPassword());
            System.out.println("数据库中的手机号（已加密）: " + savedUser.getPhone());
        }
    }
}
```

---

## 📊 任务完成检查清单

### 必做任务

- [ ] 任务1：完成环境搭建，所有测试通过
- [ ] 任务2：完成参数处理流程调试，理解执行流程
- [ ] 任务3：完成自定义ParameterHandler开发
- [ ] 任务4：编写测试报告，总结学习心得

### 选做任务

- [ ] 任务5：实现参数验证处理器
- [ ] 任务6：实现参数日志记录处理器
- [ ] 任务7：性能测试和优化

---

## 📝 学习建议

1. **循序渐进**：先完成基础测试，再进行调试和扩展
2. **动手实践**：所有代码都要亲自编写和运行
3. **深入源码**：通过调试深入理解源码实现
4. **举一反三**：基于示例开发自己的扩展功能

**预计完成时间**：2-3小时

**难度等级**：⭐⭐⭐

**前置要求**：完成前6篇文章学习
